# Aufgabenstellung
# Skript erstellen, welches eine Funktion enthält namens checkPattern
# die se funktion hat 3 parameter
# einmal einen Pfad zu einem directory
# ein dateityp .ps1, .txt, ...
# suchstring
# Als ausgabe alle zeilen aller gefundenen Dateien ausgegeben werden die den Suchstring enthalten
# Außerdem eine Liste mit erstelldatum göße(length), author/uhrheber wenns den gibt, letzte änderung
# Ausgabe wahlweise auf der Konsole(default) oder Textdatei(suchpfad) Dateinamen mit 00 anfangen lassen, damit es ganz oben ist
